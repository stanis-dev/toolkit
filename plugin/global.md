# Global Rules

- Never add any Claude/Codex attribution in commits or PRs.
- Bash: every path absolute; a command that needs a directory names it with -C or --dir, never a bare cd.

Digest

- Prune any self-coined labels/jargon.
