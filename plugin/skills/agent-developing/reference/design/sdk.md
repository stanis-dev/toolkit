# SDK concern routing

Before a code edit, load the matching canonical skill per [canonical guidance](../session/canonical-guidance.md). This page routes older links. Read every matching leaf, not the whole set.

- Tool return shape or instruction channels: [tool results](tool-results.md).
- `createAgent`, config merge, or upload-time extraction: [agent config](agent-config.md).
- Log visibility, alerting, or serializing an error: [logging](logging.md).
- Goja support, cross-turn state, clocks, or React-like APIs: [runtime and state](runtime-state.md).
- Prompt lifetime or evaluation timing: [rendering](rendering.md).

Limit code comments to three lines unless the logic is subtle.
